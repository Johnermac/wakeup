sudo apt install bat
sudo apt install fzf
sudo apt install code

usage: ./wakeup.sh <folder_path> <option>

options:
  py
  rb
  js
  php
  go
  docker

> add to check's folder if u wanna check something else

        ZZZ
  (-_-)
